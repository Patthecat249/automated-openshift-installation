module "{{ hostname }}" {
    source = "{{ dir_terraform_module_vm }}"
    vm_name = "{{ hostname }}"
    vm_cpu = "{{ cpu }}"
    vm_ram = "{{ ram }}"
    vm_vsphere_vm_boot_delay = "{{ vsphere_vm_boot_delay }}"
    vm_disk = "{{ vsphere_general_disksize }}"
}
